{{#each scripts}}
<script src="{{this}}" charset="utf-8"></script>
{{/each}}
<script src="{{assetPath}}scripts/{{#if devBuild}}main.js{{else}}main.{{webpackStats.hash}}.js{{/if}}"></script>
 <script>
       // Adaptive iframe snippet for NBCNews.com
       // note: requires jQuery
        function iframeResize() {
            var height = $('body').height();
            var location = document.location.href;
            window.parent.postMessage(["setHeight", height + 20, location], "*");
        }

      $('.question-step button').on('click', function(){
        iframeResize();
      })
        $(window).resize(iframeResize);

        $(function() {
           iframeResize();
        });



</script>
{{#if analytics.google.trackingId}}
<script>
  (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
  (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
  m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
  })(window,document,'script','//www.google-analytics.com/analytics.js','ga');
  ga('create', '{{analytics.google.trackingId}}', 'auto');
  ga('send', 'pageview');
</script>
{{/if}}
